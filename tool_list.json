[
    {
        "name": "get_current_datetime",
        "description": "Get the current date and time formatted as a readable string.",
        "inputSchema": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "get_hourly_forecast",
        "description": "Get hourly weather forecast for a specific location and date. Returns temperature, humidity, wind speed, cloud cover, and weather conditions for each hour of the specified date. If latitude and longitude is missing, it will use current location based on IP.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "current_location": {
                    "type": "boolean",
                    "description": "Whether to use the current location based on IP address. If true, 'latitude' and 'longitude' will be ignored"
                },
                "latitude": {
                    "type": "float",
                    "description": "Specified latitude (e.g., 37.7749). Ignored if 'current_location' is true. Can be searched by tool."
                },
                "longitude": {
                    "type": "float",
                    "description": "Specified longitude (e.g., -122.4194). Ignored if 'current_location' is true. Can be searched by tool."
                },
                "date": {
                    "type": "string",
                    "description": "Date for the forecast in YYYY-MM-DD format (e.g., '2026-01-15'). Must be within the available forecast range."
                }
            },
            "required": [
                "current_location",
                "latitude",
                "longitude",
                "date"
            ]
        }
    },
    {
        "name": "get_current_location",
        "description": "Get the current geographical location based on IP address. Returns latitude, longitude, city, and country information.",
        "inputSchema": {
            "type": "object",
            "properties": {},
            "required": []
        }
    },
    {
        "name": "search_and_read_web_link",
        "description": "Search the web based on the given query using SerpAPI and optionally read content from the top result links. Returns titles, links, snippets, and optionally extracted content from the pages.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Search query string to look up using SerpAPI."
                },
                "num_results": {
                    "type": "integer",
                    "description": "Number of top search results to retrieve and optionally read content from. Default is 1."
                },
                "read": {
                    "type": "boolean",
                    "description": "Whether to fetch and read content from the result links. Default is true.(If false, only titles, links, and snippets are returned.)"
                }
            },
            "required": [
                "query",
                "num_results",
                "read"
            ]
        }
    }
]